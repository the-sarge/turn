# Portfolio Go lint standard v1.0.0 — byte-identical in every portfolio repo.
# Canonical copy: the-sarge/infra config/golangci/.golangci.yml (see STANDARD.md).
# Do not edit in consuming repos; exceptions use `//nolint:<linter> // reason`.
version: "2"

run:
  timeout: 5m
  tests: true

linters:
  default: none
  enable:
    - asciicheck
    - bidichk
    - bodyclose
    - copyloopvar
    - durationcheck
    - errcheck
    - errchkjson
    - errname
    - errorlint
    - exhaustive
    - fatcontext
    - forcetypeassert
    - gocheckcompilerdirectives
    - gocritic
    - gosec
    - govet
    - ineffassign
    - intrange
    - makezero
    - mirror
    - misspell
    - nilerr
    - nilnesserr
    - nilnil
    - noctx
    - nolintlint
    - nosprintfhostport
    - predeclared
    - reassign
    - recvcheck
    - rowserrcheck
    - sloglint
    - spancheck
    - sqlclosecheck
    - staticcheck
    - testifylint
    - unconvert
    - unused
    - usestdlibvars
    - usetesting
    - wastedassign
  settings:
    errcheck:
      exclude-functions:
        - (net.Conn).Close
        - (net.Listener).Close
        - (io.Closer).Close
    exhaustive:
      default-signifies-exhaustive: true
    misspell:
      locale: US
    nolintlint:
      require-explanation: true
      require-specific: true
    staticcheck:
      checks:
        - all
        - -QF1011
        - -ST1000
  exclusions:
    generated: lax
    presets:
      - std-error-handling
    rules:
      # v1.1: the common-false-positives preset is gone so gosec G103 (audit
      # use of unsafe) applies again; unsafe is rare enough here that every
      # site deserves a //nolint with a reason. The preset's two other members
      # are kept verbatim (upstream EXC0007/EXC0010): parametrized exec and
      # variable file paths drown real findings in idiomatic Go.
      - linters:
          - gosec
        text: "G204: Subprocess launched with variable"
      - linters:
          - gosec
        text: "G304: Potential file inclusion via variable"
      # Tests may use direct type assertions and context-free requests; a
      # panic or leaked request in a test is a test failure, not a prod bug.
      - path: _test\.go
        linters:
          - forcetypeassert
          - noctx
      # File/dir permission findings are meaningless on test fixtures. All
      # other gosec rules (secrets, crypto, injection) still cover tests.
      - path: _test\.go
        linters:
          - gosec
        text: "^G30[126]"

formatters:
  enable:
    - gofumpt
    - gci
  settings:
    gci:
      sections:
        - standard
        - default
        - localmodule
  exclusions:
    generated: lax

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
